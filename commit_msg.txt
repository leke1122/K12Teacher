fix: correct wrong-question storage - write localStorage first, then async Supabase

- addWrongQuestion: write to localStorage synchronously FIRST (instant, survives page close), then async Supabase
- syncWrongQuestionsToSupabase: throw on RLS/error so localStorage fallback triggers
- deleteWrongQuestionFromSupabase: same error-throw fix
