fix: add localStorage fallback and fix session tracking for learning records

Core fixes:
- learningService: add localStorage dual-write for startLearning/endLearning/getLearningRecords/getLearningStats
  Supabase writes fail silently → data preserved in localStorage, merged on read
- knowledge page: remove currentPointIndex from useEffect deps so session doesn't restart per question
- practiceService: add localStorage fallback for addWrongQuestion, merge local+supabase on read
