fix: move formatDateLabel before useMemo in history page

Fixes "Cannot access 'K' before initialization" error in production.
formatDateLabel was used inside useMemo but defined after it,
causing TDZ (temporal dead zone) error after minification.
